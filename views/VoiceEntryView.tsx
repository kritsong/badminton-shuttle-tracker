
import React from 'react';
import { View } from '../types';

interface VoiceEntryViewProps {
  setCurrentView: (view: View) => void;
}

const VoiceEntryView: React.FC<VoiceEntryViewProps> = () => {
  return null;
};

export default VoiceEntryView;
